version: '3'

services:
  puppeteer-service:
    build: .
    ports:
      - '3002:3000'
    restart: always
    environment:
      - PORT=3002
    # Opcional: Si necesitas establecer variables de entorno adicionales
    # env_file:
    #   - .env
